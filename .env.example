# Optional. Set this to your final custom domain once connected in Vercel,
# e.g. https://www.xndr.com.au — used for canonical URLs, Open Graph tags and
# the sitemap. If unset, Vercel's production URL is used automatically.
NEXT_PUBLIC_SITE_URL=
